[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python manage.py migrate && python manage.py seed && gunicorn breathe.wsgi --workers 2 --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/auth/token/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
